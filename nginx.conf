server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;  # Necesario para aplicaciones SPA
    }

    # Si quieres agregar más configuraciones como compresión o headers, puedes hacerlo aquí.
}
